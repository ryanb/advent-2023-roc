{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "roc dev",
      "type": "shell",
      "command": "roc format ${file} && roc dev ${file}"
    },
    {
      "label": "roc test",
      "type": "shell",
      "command": "roc format ${file} && roc test ${file}"
    }
  ]
}
